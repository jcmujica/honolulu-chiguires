{
  "name": "honolulu-chiguires",
  "type": "module",
  "version": "0.0.1",
  "packageManager": "pnpm@10.17.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "preview:cf": "pnpm run build && wrangler dev",
    "deploy:cf": "pnpm run build && wrangler deploy",
    "astro": "astro"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.2.2",
    "astro": "^6.1.7",
    "tailwindcss": "^4.2.2"
  },
  "devDependencies": {
    "wrangler": "^4.83.0"
  }
}